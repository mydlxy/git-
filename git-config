gitconfig,为命令添加别名

config文件的位置，  .git/config

[core]
        repositoryformatversion = 0
        filemode = false
        bare = false
        logallrefupdates = true
        symlinks = false
        ignorecase = true
[remote "origin"]
        url = git@github.com:mydlxy/t3.git
        fetch = +refs/heads/*:refs/remotes/origin/*
[branch "master"]
        remote = origin
        merge = refs/heads/master
[alias]
        co = checkout
        br = branch
        ci = commit
        st = status

想要添加别名，在alias下将添加就可以了；
